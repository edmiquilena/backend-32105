import Coder from "./constants";
const app = express();
import express from "express";

export const numeros = [1, 2, 3, 4, 5, 6];
console.log(`esta registrado: ${Coder.registrado}`);
numeros.map((n) => n * 2);
